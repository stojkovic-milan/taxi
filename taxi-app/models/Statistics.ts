interface HeatPoint {
    point: [string, number];
}

interface ChargeDistribution {
    totalFare: Number,
    totalExtra: Number,
    totalMta: Number,
    totalTips: Number,
    totalTolls: Number,
    totalImprovment: Number,
}